"""Browser front end: a JSON-in/JSON-out facade (`bridge.py`) over the same
library the CLI and Tkinter GUI use, run inside Pyodide. No format knowledge
of its own — see `bridge.py`'s module docstring.
"""
